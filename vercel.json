{
    "build":[{
        "src":"shop/wsgi.py",
        "config": {"maxlambdaSize":"15mb", "runtime":"Python 3.12.3"}
    }],
    "routes":[
        {
            "src":"/(.*)",
            "dest":"shop/wsgi.py"
        }
    ]
}